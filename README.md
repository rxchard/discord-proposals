# Nifty Island Proposals

A simple discord bot that manages user generated proposals.

## Required environment variables

```bash
DISCORD_TOKEN=<token>
# Channel id to be used by the bot to post proposals
DISCORD_CHANNEL=<channel_id>
```
